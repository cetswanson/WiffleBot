<h3><%= @topic.title %></h3>
<p><%= @topic.user.name %></p>
<p><%= @topic.body %></p>

<%= link_to "Reply", new_topic_reply_path(@topic.id) %>

<% @topic.replies.each do |reply| %>
  <p><%= reply.body %></p>
<% end %>

